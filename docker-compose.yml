services:
  functiongemma:
    image: openmindagi/functiongemma-service:v1.0.0
    build:
      context: .
      dockerfile: docker/Dockerfile.functiongemma
    container_name: functiongemma_service
    ports:
      - "8200:8200"
    runtime: nvidia
    restart: unless-stopped
